apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podinfo-monitoring
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/VA-LEKSEEVA/AgroCD-GitOps.git
    targetRevision: main
    path: manifests/podinfo-monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: podinfo-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
